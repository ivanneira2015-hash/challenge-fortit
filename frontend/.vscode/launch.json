{
   
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: start (backend)",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}\\backend"
        }
    ]
}